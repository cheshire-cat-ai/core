# Decide protocol, host and port for your Cat. Default is http://localhost:1865
# This must be set in production so the cat is aware of its own address
# CCAT_URL=http://localhost:1865

# Database URL
# CCAT_SQL=...
# TODO TODO TODO

# Protect endpoints with a custom api key and jwt secret
# CCAT_API_KEY=meow
# CCAT_JWT_SECRET=meow_jwt

# self reload during development (should be turned to false in production)
# CCAT_DEBUG=true

# Log levels
# CCAT_LOG_LEVEL=INFO

# Uvicorn and FastAPI operating behind https proxy
# CCAT_HTTPS_PROXY_MODE=false

# CORS
# CCAT_CORS_ENABLED=true
# Comma separated list of IPs to trust with proxy headers. A wildcard '*' means always trust.
# CCAT_CORS_ALLOWED_ORIGINS=*
# Comma separated list of allowed origins or '*' to allow all
# CCAT_CORS_FORWARDED_ALLOW_IPS=*
